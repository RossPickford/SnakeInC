// #include <SDL3/SDL.h>
// #include <stdio.h>

/* typedef struct Arena
{
    void *start;
    void *currentPos;
    size_t size;
    size_t offset;
} Arena; */

// void createArena(Arena *arena, size_t size);
// void createArena(Arena *arena, size_t size);
// void RP_ArenaAlloc();