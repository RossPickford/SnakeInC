Working on Currently:

- Game Start Menu:
    IDEAS:
    - Have the snake be moving in the background, except it teleports to the opposite wall on collision
    - Have the keys that are shown shrink and come back to normal size when pressed.
    - Make the snake respond to movement.



UI Editor:

- create a layout - made the input textbox. TO DO:
    - create a window that opens in the centre to insert the name of the layout file, and then a button to chose the file to save it in.
    - Add backspace capability
    - create an enum that allows you to select key input type (e.g. spaces allowed or not)



WIDGETS:

- button
- Image
- Input Text box
- Slider
- Drop Down Menu

BUTTON:
- Normal Image
- Hover Image
- Selected Image
- rect ()
