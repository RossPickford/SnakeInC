Working on Currently:

- Game Start Menu:
    IDEAS:
    - Have the snake be moving in the background, except it teleports to the opposite wall on collision
    - Have the keys that are shown shrink and come back to normal size when pressed.
    - Make the snake respond to movement.



UI Editor:

- create a layout - made the input textbox. TO DO:
    - create a window that opens in the centre to insert the name of the layout file, and then a button to chose the file to save it in.
    - Add backspace capability
    - create an enum that allows you to select key input type (e.g. spaces allowed or not)



WIDGETS:

- button
- Image
- Input Text box
- Slider
- Drop Down Menu

BUTTON:
- Normal Image
- Hover Image
- Selected Image
- rect ()


Create a DrawCommand struct that stores the texture pointer and the rect - then the render buffer can store just the drawcommand structs (from an arena down the line?)

What am I stuck on?

- Allocating widgets to an arena, specifically the order to allocate, and how to allocate (in terms of structs and what data should be used)
- There are buttons and display widgets.

- UI elements in groups are linearly allocated. If a UI element has multiple display types - they should be linearly allocated and pointed 
    to by the void pointer in the UI elements struct. You should not create different structs for display types that have the same function and
    are tied together by their function (i.e. they are a button). The colours for a button function 